<%= render "loader" %>

<%= javascript_tag do %>
  var homeConfig = function() {
    return (
      <%= { sign_in_available: !admin_signed_in? && !candidate_signed_in? && !recruiter_signed_in? }.to_json.html_safe %>
    )
  }
<% end %>

<%= content_tag(:div, "", id: "home", class: 'smoothly-show-page', data: {
  controller: "home"
}) %>
