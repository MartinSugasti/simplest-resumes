<% content_for :top_section do %>
  <h2 class="mb-1"><%= t('dashboard.requests') %></h2>
  <p class="small text-muted mb-0"><%= t('dashboard.invitations') %> / <strong><%= t('dashboard.requests') %></strong></strong></p>
<% end %>

<%= content_tag(:div, '', id: 'admins-invitation-requests-container', data: {
  controller: 'admins_invitation_requests'
}) %>