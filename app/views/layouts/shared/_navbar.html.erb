<%= javascript_tag do %>
  var navbarConfig = function() {
    return (
      <%= { user_type: current_user.class.name.downcase }.to_json.html_safe %>
    )
  }
<% end %>

<%= content_tag(:nav, '', id: 'dashboard-navbar', data: {
  controller: 'navbar'
}) %>