<%= javascript_tag do %>
  var navbarConfig = function() {
    return (
      <%= { user: current_user }.to_json.html_safe %>
    )
  }
<% end %>

<%= content_tag(:nav, "", id: "navbar", class: "bg-primary-dark", data: {
  controller: "navbar"
}) %>