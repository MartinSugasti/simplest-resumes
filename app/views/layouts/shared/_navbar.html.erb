<%= javascript_tag do %>
  var navbarConfig = function() {
    return (
      <%= { user: user }.to_json.html_safe %>
    )
  }
<% end %>

<%= content_tag(:nav, "", id: "dashboard-navbar", data: {
  controller: "navbar"
}) %>