<% content_for :top_section do %>
  <h2 class="mb-1"><%= t('dashboard.my_resume') %></h2>
  <p class="small text-muted mb-0"><strong><%= t('dashboard.my_resume') %></strong></p>
<% end %>

<%= content_tag(:div, '', id: 'candidates-my-resume-container', data: {
  controller: 'candidates_my_resume'
}) %>
